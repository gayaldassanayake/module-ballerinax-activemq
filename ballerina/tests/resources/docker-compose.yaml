services:
  activemq:
    image: 'apache/activemq-classic:6.2.0'
    container_name: activemq-test-server
    ports:
      - '61616:61616'  # OpenWire protocol
      - '8161:8161'    # Web console
    environment:
      - ACTIVEMQ_BROKER_NAME=testbroker
      - ACTIVEMQ_ADMIN_LOGIN=admin
      - ACTIVEMQ_ADMIN_PASSWORD=admin
      - ACTIVEMQ_MIN_MEMORY=512
      - ACTIVEMQ_MAX_MEMORY=2048
    healthcheck:
      test: ["CMD-SHELL", "curl -f -u admin:admin http://localhost:8161/admin/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - activemq-test-network

  activemq-ssl:
    image: 'apache/activemq-classic:6.2.0'
    container_name: activemq-ssl-test-server
    ports:
      - '61617:61617'  # SSL OpenWire protocol
      - '8162:8161'    # Web console
    environment:
      - ACTIVEMQ_BROKER_NAME=testbroker-ssl
      - ACTIVEMQ_ADMIN_LOGIN=admin
      - ACTIVEMQ_ADMIN_PASSWORD=admin
      - ACTIVEMQ_MIN_MEMORY=512
      - ACTIVEMQ_MAX_MEMORY=2048
    volumes:
      - ./secrets/server-keystore.jks:/opt/apache-activemq/conf/broker.ks:ro
      - ./secrets/server-truststore.jks:/opt/apache-activemq/conf/broker.ts:ro
      - ./configs/activemq-ssl.xml:/opt/apache-activemq/conf/activemq.xml:ro
    healthcheck:
      test: ["CMD-SHELL", "test -f /opt/apache-activemq/data/activemq.pid && ps aux | grep -v grep | grep -q '[a]ctivemq'"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - activemq-test-network

networks:
  activemq-test-network:
    driver: bridge
